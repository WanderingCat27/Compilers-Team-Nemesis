x = 10
y = 3.6 plus 4.5 *Adds two decimals together
z = 35
define addTwoNumbers (numOne, numTwo) {
    x=numOne
    y=numTwo
    return x plus y
}
addTwoNumbers (x, z) * Adds x and z together
x=notexists
