a = "a"

b=1

define f() {
    c=7
    a="not a"
    is 3 equal to 3 {
        c=3
        * additional scope for in if (counted by levels)
        in_if="in if (level 2)" 
    }
    * if has ended so everything in if scope is removed from memory; this will define a completely new variable
    in_if=1
}

b=c
